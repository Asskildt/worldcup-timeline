# Roadmap – fotballvm.asskildt.eu

## Status

FIFA World Cup 2026 schedule. Live in production at [fotballvm.asskildt.eu](https://fotballvm.asskildt.eu).

**Implemented:**
- Timeline, table, and groups view with live results
- KO bracket — visual knockout tree from R32 to the final, with match-number badge and via-label
- Arena map — SVG map of all 16 venues, click to open venue modal
- TV listings — NRK / TV 2 per match
- Favorites filter (timeline + table) with potential KO paths highlighted
- Group modal with standings and upcoming matches
- Team modal with full match list
- Rest-day toggle in table view
- Timeline expanded/compact mode
- Flag SVGs from lipis/flag-icons via sprite generated by build.js
- Host country flags (USA, Canada, Mexico) in header
- Statistics tab (hidden — data not yet available)
- Match modal with venue info, scorers, extra time, penalties
- Sharing via URL/hash + OG tags per match
- Test scenarios (group / r32 / sf)
- `resolveKOTeams` + `getTeamBracketPaths` — reusable bracket logic

---

## Priority table

| Task | Value | Complexity | Status |
|------|-------|------------|--------|
| **app.js modularization** | High | High | — |
| **Multi-team filter** | Medium | Medium | — |
| **Playoff integration** | Low | Medium | — |
| Internationalization (timezone/language) | Low | High | — |
| Open source prep (LICENSE, footer link) | Low | Low | — |
| OG image update | Low | Minimal | — |
| Offline support (service worker) | Low | Medium | — |

---

## Next: app.js modularization

`app.js` is ~3000 lines. Natural split points:

```
filter.js   — ACTIVE_FILTER, setTeamFilter, getTeamBracketPaths
timeline.js — buildTimeline, renderTlToolbar
table.js    — buildTable, renderTblToolbar
bracket.js  — buildBracket, matchCard
modals.js   — openModal, openVenueModal, openTeamModal, openGroupModal
groups.js   — buildGroups, buildStats, buildArenas
ui.js       — updateCountdown, theme, CRT, buildNorwaySchedule
```

Requires either ES modules (import/export) or file concatenation in `build.js`. Best timing: after the tournament starts, during a natural feature pause.

---

## Multi-team filter

Let users select multiple teams in the filter simultaneously (max one group, but multiple individual teams).

- `ACTIVE_FILTER` changes from `{ type, value }` to `{ type: 'teams'|'group', values: Set<string> }`
- Click on a team in the filter list: toggle (add/remove from set), not replace
- Filter button shows a flag row for selected teams, with `+N` if many
- "Clear" removes all
- Group filter remains exclusive (one at a time)
- Special button: "Show favourites" — sets `values` to all favourite teams at once
- ~6–8 places in the code that check `=== ACTIVE_FILTER.value` must be updated to `.has()`

See `ideer.md` for full UX discussion.

---

## Internationalization

Future idea: show times in the visitor's local timezone, and optionally switch UI language. Lower priority — the site is Norwegian-focused by design.

---

## Done and no longer relevant

- ~~Calendar export (.ics)~~ — not prioritised
- ~~Link from asskildt.eu~~ ✅ — done
- ~~Playoff tab~~ — low priority, data exists but not requested
- ~~Build script~~ ✅ — done
- ~~KO bracket~~ ✅ — done
- ~~Arena map~~ ✅ — done
- ~~TV listings~~ ✅ — done
- ~~Favorites filter~~ ✅ — done
- ~~SVG flags~~ ✅ — done
- ~~Host country flags in header~~ ✅ — done
- ~~Rest-day toggle~~ ✅ — done
- ~~Timeline expanded view~~ ✅ — done
- ~~Group modal~~ ✅ — done
