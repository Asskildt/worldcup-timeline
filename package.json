{
  "name": "em28",
  "version": "1.0.0",
  "description": "UEFA EM 2028 kampprogram — em28.asskildt.eu",
  "scripts": {
    "build": "node build.js",
    "build:en": "node build.js && node build.js --lang=en",
    "build:all": "node build.js && node build.js --lang=en",
    "watch": "nodemon --watch src --ext html,css,js,json --exec 'node build.js'"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
